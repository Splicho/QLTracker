[variables]
PNPM_STORE_DIR = "/root/.local/share/pnpm/store"

[phases.install]
cmds = ["pnpm install --frozen-lockfile --store-dir /root/.local/share/pnpm/store"]

[caches]
paths = ["/root/.local/share/pnpm/store"]
